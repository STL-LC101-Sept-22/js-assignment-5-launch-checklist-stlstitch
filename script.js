
const { formSubmission } = require("./scriptHelper");

window.addEventListener("load", function() {
console.log("test")
   let listedPlanets;
   // Set listedPlanetsResponse equal to the value returned by calling myFetch()
//    let listedPlanetsResponse(myFetch("https://handlers.education.launchcode.org/static/planets.json").then.(function)response));
//    listedPlanetsResponse.then(function (result) {
//     listedPlanets = result;
//     console.log(listedPlanets);
// }).then(function () {
//     console.log(listedPlanets);
//     let selectedPlanet = pickPlanet(listedPlanets);
//     addDestinationInfo(document, selectedPlanet.name, selectedPlanet.diameter, selectedPlanet.star, selectedPlanet.distance, selectedPlanet.moons, selectedPlanet.image);  
// })

   


form.addEventListener("submit", function(event) {

    let pilotStatus = document.querySelector("input[pilotName=pilot]");
    let coPilotStatus = document.querySelector("input[copilotName=copilot]");
    let fuelStatus = document.querySelector("input[fuelStatus=fuelLevel]");
    let cargoStatus = document.querySelector("input[cargoStatus=cargoLevel]");
console.log(pilotStatus)
})
})